name: Update Sprite Version

on:
  schedule:
    - cron: "0 * * * *" # Run every hour
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write
  pull-requests: write

jobs:
  check-and-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v21

      - name: Run update script
        run: |
          ./scripts/update-version.sh

      - name: Check for changes
        id: check
        run: |
          if git diff --quiet version.json; then
            echo "update_needed=false" >> $GITHUB_OUTPUT
          else
            CURRENT_VERSION=$(git show HEAD:version.json | jq -r '.version')
            LATEST_VERSION=$(jq -r '.version' version.json)
            echo "current=$CURRENT_VERSION" >> $GITHUB_OUTPUT
            echo "latest=$LATEST_VERSION" >> $GITHUB_OUTPUT
            echo "update_needed=true" >> $GITHUB_OUTPUT
          fi

      - name: Build package
        if: steps.check.outputs.update_needed == 'true'
        run: |
          nix build --print-build-logs

      - name: Verify installation
        if: steps.check.outputs.update_needed == 'true'
        run: |
          ./result/bin/sprite --version

      - name: Create Pull Request
        if: steps.check.outputs.update_needed == 'true'
        id: create-pr
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: update sprite to version ${{ steps.check.outputs.latest }}"
          title: "chore: update sprite to version ${{ steps.check.outputs.latest }}"
          body: |
            ## Automated Sprite Update

            This PR updates Sprite from version `${{ steps.check.outputs.current }}` to `${{ steps.check.outputs.latest }}`.

            ### Changes
            - Updated `version` in `version.json`
            - Updated `sha256` hashes for all platforms

            To test this update locally:
            ```bash
            nix build
            ./result/bin/sprite --version
            ```

            This PR was automatically generated by the update workflow.
          branch: update-sprite-${{ steps.check.outputs.latest }}
          delete-branch: true

      - name: Merge Pull Request
        if: steps.check.outputs.update_needed == 'true' && steps.create-pr.outputs.pull-request-number
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr merge ${{ steps.create-pr.outputs.pull-request-number }} --auto --rebase --delete-branch
